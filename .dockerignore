# Used when building with repo root as context (optional). Worker build uses backend/.dockerignore:
#   docker build -f backend/Dockerfile ./backend
# Does not affect Netlify (Next.js build does not use this file).

.git
.next
node_modules
backend/node_modules
backend/dist
coverage
.env
.env.*
